/**
 * Provider which provides functions that generate random values.
 */
provider "random" {}

/**
 * Provider which provides functions that interact with the Azure Resource Manager.
 */
provider "azurerm" {
	features {}
}